name: Waka Readme

on:
  # for manual workflow trigger
  workflow_dispatch:
  schedule:
     # Runs at 12:00am UTC +5:30
    - cron: "30 18 * * *"

jobs:
  update-readme:
    name: WakaReadme DevMetrics
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master # this action name
        with:
          WAKATIME_API_KEY: waka_d0ee7046-d25d-49e0-8196-82fe594e223a
          SHOW_TITLE: true
          SHOW_TOTAL: true
